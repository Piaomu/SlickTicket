@model SlickTicket.Models.ViewModels.ProjectManagerViewModel

@{
    ViewData["Title"] = "Assign Users";
}

<div class="container">
    <h2> Assign Project Manager </h2>

    <div class="row">
        <div class="col-md-4">
            <div class="panel panel-green margin-bottom-40">
                <div class="panel-heading">
                    <h3 class="panel-title"> @Model.Project.Name</h3>
                </div>
            </div>
            <p>
                Name :
                <span>@Model.Project.Name</span>
            </p>

            <form asp-action="AssignPM" asp-controller="Projects" method="post">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <input type="hidden" asp-for="@Model.Project.Id">
                <input type="hidden" asp-for="@Model.Project.Name">
                <div class="col-12">
                    <div class="form-group">
                        <label asp-for="@Model.Users" class="control-label">Assign Project Manager: @Model.Project.Name</label>
                        <select asp-for="@Model.SelectedUser" class="form-control" asp-items="@Model.Users"></select>
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-lg-offset-2 col-lg-10">
                        <input type="submit" class="btn btn-success" value="Assign Project Manager" />
                    </div>
                </div>
            </form>


        </div>
        <!--End Hover Rows-->
    </div>
    <div>
        @Html.ActionLink("Back to List", "Index")
    </div>
</div>
